authn:
  - type: "facebook"
    authZ: jwt
    config:
      collection: oauth2
      storage: db
      client_id: "${FACEBOOK_CLIENT_ID}"
      client_secret: "${FACEBOOK_SECRET}"
      scopes:
        - "email"
      fields:
        - "email"