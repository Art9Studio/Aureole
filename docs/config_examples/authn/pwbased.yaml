authN:
  - type: "password_based"
    authZ: jwt
    config:
      main_hasher: argon2
      storage: db
      password_reset:
        sender: email
        template: reset
        exp: 3600
      verification:
        sender: email
        template: email_verification
        exp: 3600